---
phase: 02-documentation-overhaul
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-documentation-overhaul/fragments/template-reference.md
autonomous: true
requirements:
  - DOCS-04
tags:
  - documentation
  - obsidian
  - templater
  - reference

must_haves:
  truths:
    - "Every one of the 11 templates has its own subsection in the reference"
    - "Each template entry states what the note contains, what prompts appear, and when to use the template"
    - "The Concept Note Template entry contains a warning callout about the silent-overwrite risk (AUDIT critical violation 1, per D-08)"
    - "The Update Note Status entry contains a warning callout about the missing 'raw' status (AUDIT critical violation 2, per D-08)"
    - "The prompt quick-reference table from the current README is included as a visible table (not inside <details>)"
  artifacts:
    - path: ".planning/phases/02-documentation-overhaul/fragments/template-reference.md"
      provides: "Self-contained Template Reference section covering all 11 templates"
      contains: "## Template Reference"
  key_links:
    - from: "Each template subsection"
      to: "the actual file in _templates/"
      via: "matching template name in the heading"
      pattern: "### "
---

## Phase Goal

**As a** fellow student looking up what a template does, **I want to** scan one place that documents every template's purpose, prompts, and caveats, **so that** I can pick the right one without opening source files.

<objective>
Write the Template Reference section (DOCS-04) as a self-contained Markdown fragment. Covers **all 11 templates** (D-05), grouped by purpose, with a uniform per-entry layout and the two AUDIT-critical callouts (D-08).

Purpose: DOCS-04 — reorganize the current README's narrative Usage section into a scannable reference, add the two missing templates (`University Dashboard`, `Mark Reviewed`), and unwrap the `<details>`-wrapped prompts table.
Output: `.planning/phases/02-documentation-overhaul/fragments/template-reference.md` — a complete `## Template Reference` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-documentation-overhaul/02-CONTEXT.md
@.planning/phases/02-documentation-overhaul/02-RESEARCH.md
@CLAUDE.md
@README.md
@AUDIT.md
@_templates/University Dashboard.md
@_templates/Mark Reviewed.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Template Reference fragment covering all 11 templates</name>
  <files>.planning/phases/02-documentation-overhaul/fragments/template-reference.md</files>
  <read_first>
    - README.md (current — read the entire Usage section lines 130-291 including the `<details>` prompts table; this is the raw material for 9 of the 11 entries)
    - _templates/University Dashboard.md (FULL — not documented in current README; read to write its entry)
    - _templates/Mark Reviewed.md (FULL — not documented in current README; read to write its entry)
    - AUDIT.md (skim Sections 1-3, but the two specific critical violations to surface are summarized in 02-RESEARCH.md section "Audit Findings — Student-Visible Issues (D-08)")
    - .planning/phases/02-documentation-overhaul/02-CONTEXT.md (decisions D-02, D-05, D-08)
    - .planning/phases/02-documentation-overhaul/02-RESEARCH.md (sections "Template Inventory (Complete — 11 Templates)", "Audit Findings — Student-Visible Issues (D-08)", "Template Reference Entry Format")
    - CLAUDE.md (project — reader persona, jargon rules)
  </read_first>
  <action>
    Create the file `.planning/phases/02-documentation-overhaul/fragments/template-reference.md` containing the **complete** `## Template Reference` section.

    The fragment MUST contain, in this order:

    1. Top-level heading: `## Template Reference`
    2. One-paragraph intro: state that this section is a lookup table for all 11 templates, grouped into three categories — Creation Templates (note-making), Utility Templates (operations on existing notes), and Dashboards. Each entry follows the same shape: Creates / Prompts / When to use / notes.
    3. `### Prompt Quick Reference` subsection — a Markdown table summarizing prompt behavior for every template. Use the existing table from the current README (lines 280-291) as a base, BUT (a) it is no longer inside `<details>`, and (b) it MUST include rows for the two missing templates: `Mark Reviewed` and `University Dashboard`. Columns: Template | Prompts for subject? | Prompts for year? | Prompts for parcial? | Prompts for tema?
    4. `### Creation Templates` subsection containing one `####` entry per template in this exact order: `Lecture Note`, `Concept Note Template`, `General Note`, `Subject Hub`, `Parcial Prep Note`.
    5. `### Utility Templates` subsection containing one `####` entry per template in this exact order: `Assign Tema to Current Note`, `Link Concepts to Note`, `Quick Create Concept`, `Update Note Status`, `Mark Reviewed`.
    6. `### Dashboards` subsection containing one `####` entry: `University Dashboard`.

    Each of the 11 `#### <Template Name>` entries MUST follow this uniform structure:
    ```
    #### <Template Name>

    **Creates:** <one sentence — what the resulting note/effect is>
    **Prompts:** <ordered prompt sequence, e.g., year picker -> subject picker -> tema picker -> topic>
    **When to use:** <one sentence>

    <2-3 sentence description of behavior, expected starting context, and important caveats>

    <Optional frontmatter example code block — include for entries that have one in the current README>
    ```

    Source material for each entry:
    - **Lecture Note, Concept Note Template, General Note, Subject Hub, Parcial Prep Note (Study Guide), Assign Tema to Current Note, Link Concepts to Note, Quick Create Concept, Update Note Status** — use the corresponding Usage subsection in the current README (lines 132-275) as the basis; reformat into the uniform structure above. Preserve frontmatter code blocks that already exist in the current README.
    - **Mark Reviewed** — write from reading `_templates/Mark Reviewed.md`. Describe what it sets in frontmatter, that it schedules the next review using `schema.reviewIntervals` (`easy: 14`, `medium: 7`, `hard: 3`, `blank: 1` days), and that it is used in spaced-repetition review sessions on concept notes.
    - **University Dashboard** — write from reading `_templates/University Dashboard.md`. Describe what dashboards it shows (vault-wide views) and when to use it (course overview / semester planning).

    Two mandatory callouts (D-08) — embed inline within the relevant entries using a blockquote that starts with `> **Heads up:**`:

    (a) **Inside the `#### Concept Note Template` entry**, after the description, add a blockquote:
    `> **Heads up:** Always run this template from an untitled note, or via the command palette **"Templater: Create new note from template"**. Running it on an existing named note will overwrite that file without warning — this is a known issue tracked in AUDIT.md.`

    (b) **Inside the `#### Update Note Status` entry**, after the description, add a blockquote:
    `> **Heads up:** If notes with `status: raw` do not appear in the picker, this is a known issue — the fallback status list is missing `"raw"`. As a workaround, update those notes' frontmatter manually until a fix is released. Tracked in AUDIT.md.`

    Tone & content rules (mandatory):
    - No `<details>` or `<summary>` tags (per D-02). The prompts table that was inside `<details>` must be visible.
    - No developer jargon: forbidden tokens — `CommonJS`, `module.exports`, `factory function`, `EROFS`, `require()`, `async/await`, `processFrontMatter` (the current README mentions this; rewrite as "the template writes frontmatter in a single batch update").
    - Wrap template names in `code` style or **bold** consistently within prose.
    - All 11 template names MUST appear exactly as the filenames (without `.md`): `Lecture Note`, `Concept Note Template`, `General Note`, `Subject Hub`, `Parcial Prep Note`, `Assign Tema to Current Note`, `Link Concepts to Note`, `Quick Create Concept`, `Update Note Status`, `Mark Reviewed`, `University Dashboard`.
    - Do not include any other section (no Setup Guide, no Config Guide, no Walkthrough). Only the Template Reference.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "^## Template Reference$" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "### Prompt Quick Reference" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "### Creation Templates" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "### Utility Templates" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "### Dashboards" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### Lecture Note" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### Concept Note Template" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### General Note" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### Subject Hub" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### Parcial Prep Note" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### Assign Tema to Current Note" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### Link Concepts to Note" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### Quick Create Concept" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### Update Note Status" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### Mark Reviewed" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "#### University Dashboard" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "Heads up" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "overwrite" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; grep -q "raw" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; ! grep -q "&lt;details&gt;" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; ! grep -qE "CommonJS|module\.exports|processFrontMatter" .planning/phases/02-documentation-overhaul/fragments/template-reference.md &amp;&amp; [ "$(grep -c '^#### ' .planning/phases/02-documentation-overhaul/fragments/template-reference.md)" -eq 11 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-documentation-overhaul/fragments/template-reference.md` exists
    - File contains heading `## Template Reference` (exact match)
    - File contains subsections `### Prompt Quick Reference`, `### Creation Templates`, `### Utility Templates`, `### Dashboards`
    - File contains exactly 11 `#### ` (level-4) headings — one per template
    - Each of these exact template names appears as a `#### ` heading: `Lecture Note`, `Concept Note Template`, `General Note`, `Subject Hub`, `Parcial Prep Note`, `Assign Tema to Current Note`, `Link Concepts to Note`, `Quick Create Concept`, `Update Note Status`, `Mark Reviewed`, `University Dashboard`
    - Each `#### ` entry contains the labels `**Creates:**`, `**Prompts:**`, and `**When to use:**`
    - The Concept Note Template entry contains a `> **Heads up:**` blockquote mentioning "overwrite"
    - The Update Note Status entry contains a `> **Heads up:**` blockquote mentioning the `raw` status
    - The Prompt Quick Reference table includes rows for `Mark Reviewed` and `University Dashboard` (not just the original 9)
    - File does NOT contain `<details>` or `<summary>` tags
    - File does NOT contain the tokens `CommonJS`, `module.exports`, `processFrontMatter`, `EROFS`
    - File does NOT contain a `## Setup Guide` or `## Configuration Guide` or `## Workflow Walkthrough` heading
  </acceptance_criteria>
  <done>The Template Reference fragment lists all 11 templates in a uniform, scannable format with the two AUDIT-critical callouts. A student can find any template by name and learn its purpose, prompts, and gotchas without opening another file.</done>
</task>

</tasks>

<verification>
- Fragment file exists at expected path
- All acceptance criteria pass — including the exact count of 11 `#### ` headings
- Manual scan: every template in `_templates/` has a matching entry
</verification>

<success_criteria>
DOCS-04 satisfied as a standalone section. Phase 2 success criterion #4 ("Template reference covers all 11 templates with purpose and when-to-use") and success criterion #6 ("audit findings referenced where the audit surfaced notable issues") are achievable from this fragment.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-overhaul/02-04-SUMMARY.md`.
</output>
