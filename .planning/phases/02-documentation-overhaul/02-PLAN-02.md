---
phase: 02-documentation-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-documentation-overhaul/fragments/config-guide.md
autonomous: true
requirements:
  - DOCS-02
tags:
  - documentation
  - obsidian
  - templater
  - config

must_haves:
  truths:
    - "A student can identify every top-level config key (fs, labels, years, parciales, codeLanguage, features, schema) and what it controls"
    - "The guide explains how to add a subject (subjects are emergent from prompts, not listed in config) and how to add/remove years"
    - "The guide explains how to toggle features.parcial and what changes when it is true vs false"
    - "The guide contains one concrete before/after worked example using exact key paths from universityConfig.js"
  artifacts:
    - path: ".planning/phases/02-documentation-overhaul/fragments/config-guide.md"
      provides: "Self-contained Configuration Guide section ready to splice into README.md"
      contains: "## Configuration Guide"
  key_links:
    - from: "Config Guide key reference table"
      to: "_templater_scripts/universityConfig.js"
      via: "exact key names (fs.universityRoot, labels.tema, years, features.parcial, schema.statuses)"
      pattern: "fs\\.universityRoot"
---

## Phase Goal

**As a** fellow student adapting this vault to my own curriculum, **I want to** edit `universityConfig.js` with confidence, **so that** I can rename folders/labels and adjust years without breaking the templates.

<objective>
Write the Configuration Guide section (DOCS-02) as a self-contained Markdown fragment. Standalone-usable: a student can read just this fragment and successfully customize their config.

Purpose: DOCS-02 — replace the buried `<details>`-wrapped "Common tweaks" block with a visible, structured Config Guide that includes a concrete before/after worked example (D-06).
Output: `.planning/phases/02-documentation-overhaul/fragments/config-guide.md` — a complete `## Configuration Guide` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-documentation-overhaul/02-CONTEXT.md
@.planning/phases/02-documentation-overhaul/02-RESEARCH.md
@CLAUDE.md
@README.md
@_templater_scripts/universityConfig.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Configuration Guide fragment with before/after example</name>
  <files>.planning/phases/02-documentation-overhaul/fragments/config-guide.md</files>
  <read_first>
    - _templater_scripts/universityConfig.js (FULL — this is the source of truth for every key name, default value, and comment; the worked example must match the actual nested structure exactly)
    - README.md (current — read lines 82-128, the "Configure & Customize" section and the `<details>` "Common tweaks" block being replaced)
    - .planning/phases/02-documentation-overhaul/02-CONTEXT.md (decisions D-01, D-02, D-06)
    - .planning/phases/02-documentation-overhaul/02-RESEARCH.md (section "universityConfig.js — Complete Key Reference" and "Recommended worked example for D-06")
    - CLAUDE.md (project — reader persona, jargon rules)
  </read_first>
  <action>
    Create the file `.planning/phases/02-documentation-overhaul/fragments/config-guide.md` containing the **complete** `## Configuration Guide` section.

    The fragment MUST contain, in this order:

    1. Top-level heading: `## Configuration Guide`
    2. One paragraph intro: state that `_templater_scripts/universityConfig.js` is the single source of truth — every template reads it at runtime, so editing this one file changes folder names, prompt labels, year options, and exam-period behavior across the whole workflow. Mention students must reload Templater scripts (or restart Obsidian) after editing the file.
    3. `### Anatomy of universityConfig.js` subsection — explain the top-level structure with these exact group names: `fs` (filesystem folder names), `labels` (prompt text shown in pickers), `years` (the year picker options), `parciales` (the exam-period picker options), `codeLanguage` (default code fence language for Lecture Notes), `features` (feature toggles, currently just `parcial`), `schema` (note types, statuses, and review intervals).
    4. `### Key Reference` subsection — a Markdown table with columns "Key path | Default | What it controls". Include exactly these rows, with default values copied verbatim from `universityConfig.js`:
       - `fs.universityRoot` — `"Universidad"` — Base folder in vault root where all university notes live
       - `fs.parcialContainer` — `"Parciales"` — Container folder for exam-period subfolders (only used when `features.parcial` is true)
       - `fs.temaContainer` — `"Temas"` — Container folder for topic/module subfolders inside each subject
       - `labels.subject` — `"Subject"` — Text shown in the subject picker prompt
       - `labels.year` — `"Year"` — Text shown in the year picker prompt
       - `labels.parcial` — `"Parcial"` — Text shown in the exam-period picker (rename to `"Semester"` or `"Term"`)
       - `labels.final` — `"Final"` — Label for the final-exam option in the parcial picker
       - `labels.tema` — `"Tema"` — Text shown in the tema picker (rename to `"Module"`, `"Topic"`, etc.)
       - `labels.general` — `"General"` — Catch-all label used when no specific tema is chosen
       - `years` — `["Year 1", "Year 2", "Year 3", "Year 4", "Year 5"]` — Options shown in the year picker
       - `parciales` — `["General", "Parcial 1", "Parcial 2", "Parcial 3", "Final"]` — Options shown in the exam-period picker
       - `codeLanguage` — `""` — Default language for Lecture Note code fences (empty = language-neutral)
       - `features.parcial` — `false` — When `true`, enables the exam-period prompt and the `Parciales/` folder layer
       - `schema.types` — object (`lecture`, `concept`, `subject-hub`, `parcial-prep`, `general`, `university-dashboard`) — Note type identifiers used in frontmatter and Dataview filters
       - `schema.statuses` — `["raw", "draft", "reviewed", "complete"]` — Workflow stages used by the Update Note Status picker
       - `schema.reviewIntervals` — `{ easy: 14, medium: 7, hard: 3, blank: 1 }` — Days until next review for Mark Reviewed
    5. `### Common Customizations` subsection — unwrap the previous `<details>` content as visible bullet points covering: change the base folder name, rename Temas, switch label language, adjust years, adjust parciales, set default code fence language, customize statuses, enable exam-period grouping.
    6. `### Worked Example: English curriculum with semesters` subsection — a concrete before/after using the scenario from RESEARCH.md "Recommended worked example for D-06":
       - **Scenario sentence:** "You are switching from the Spanish defaults to an English vocabulary AND moving from a 5-year curriculum to a 3-year curriculum AND turning on semester-based exam-period grouping."
       - **Before** code block: the relevant slices of the default config (use only the fields being changed plus their immediate parents, for readability). Show `fs.universityRoot: "Universidad"`, `labels.tema: "Tema"`, `labels.parcial: "Parcial"`, `years: ["Year 1", "Year 2", "Year 3", "Year 4", "Year 5"]`, `features: { parcial: false }`.
       - **After** code block: the same slices with new values: `fs.universityRoot: "University"`, `labels.tema: "Module"`, `labels.parcial: "Semester"`, `years: ["Year 1", "Year 2", "Year 3"]`, `features: { parcial: true }`.
       - **What changes** — three bullets describing the observable behavioral diffs: (a) new notes land under a `University/` root folder; (b) prompts say "Module" instead of "Tema" and "Semester" instead of "Parcial"; (c) Parcial Prep Note now asks for the semester and places the file inside `Parciales/<Semester>/` (note: `fs.parcialContainer` is unchanged, so the container is still literally named "Parciales" unless they also change that — call this out explicitly).
    7. A short closing paragraph reminding the student to reload Templater after editing the file.

    Tone & content rules (mandatory):
    - No `<details>` or `<summary>` tags anywhere (per D-02).
    - No developer jargon: forbidden tokens — `CommonJS`, `module.exports`, `factory function`, `require()`. "Config file" is fine; "schema" is fine because it is a key name.
    - All key paths and default values MUST match `_templater_scripts/universityConfig.js` exactly. Use ASCII straight quotes in code blocks (the existing README has smart quotes — do not copy that bug).
    - Do not include any other section (no Setup Guide, no Walkthrough). Only the Configuration Guide.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "^## Configuration Guide$" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "### Anatomy of universityConfig.js" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "### Key Reference" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "### Worked Example" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "fs.universityRoot" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "features.parcial" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "schema.statuses" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "Universidad" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "University" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; grep -q "Semester" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; ! grep -q "&lt;details&gt;" .planning/phases/02-documentation-overhaul/fragments/config-guide.md &amp;&amp; ! grep -qE "CommonJS|module\.exports" .planning/phases/02-documentation-overhaul/fragments/config-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-documentation-overhaul/fragments/config-guide.md` exists
    - File contains heading `## Configuration Guide` (exact match)
    - File contains subsections `### Anatomy of universityConfig.js`, `### Key Reference`, `### Common Customizations`, `### Worked Example` (each as `###` heading)
    - Key Reference section contains every one of these literal key strings: `fs.universityRoot`, `fs.parcialContainer`, `fs.temaContainer`, `labels.subject`, `labels.year`, `labels.parcial`, `labels.final`, `labels.tema`, `labels.general`, `years`, `parciales`, `codeLanguage`, `features.parcial`, `schema.types`, `schema.statuses`, `schema.reviewIntervals`
    - Default values shown in Key Reference match universityConfig.js exactly — `"Universidad"`, `"Parciales"`, `"Temas"`, `"Tema"`, `"General"`, `false` for features.parcial, `["raw", "draft", "reviewed", "complete"]` for schema.statuses
    - Worked example contains both `"Universidad"` (before) and `"University"` (after)
    - Worked example contains both `"Tema"` (before) and `"Module"` (after)
    - Worked example contains both `"Parcial"` (before) and `"Semester"` (after)
    - File does NOT contain `<details>` or `<summary>` tags
    - File does NOT contain the tokens `CommonJS` or `module.exports`
    - File does NOT contain a `## Setup Guide` or `## Walkthrough` or `## Template Reference` heading
  </acceptance_criteria>
  <done>The Configuration Guide fragment is a complete standalone section. A student following only this fragment can identify every config knob, find the default values, and execute the worked example to validate their understanding.</done>
</task>

</tasks>

<verification>
- Fragment file exists at expected path
- All acceptance criteria pass
- Manual cross-check: every key name in the Key Reference table appears verbatim in `_templater_scripts/universityConfig.js`
</verification>

<success_criteria>
DOCS-02 satisfied as a standalone section. Phase 2 success criterion #2 ("Config guide covers editing universityConfig.js for custom subjects, years, and folder paths with a worked example") is achievable from this fragment.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-overhaul/02-02-SUMMARY.md`.
</output>
