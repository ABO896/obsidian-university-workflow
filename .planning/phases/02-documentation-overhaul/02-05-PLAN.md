---
phase: 02-documentation-overhaul
plan: 05
type: execute
wave: 2
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-05
tags:
  - documentation
  - obsidian
  - integration

must_haves:
  truths:
    - "README.md sections appear in D-03 order: TL;DR / intro -> Setup -> Config Guide -> Walkthrough -> Template Reference -> Troubleshooting/FAQ"
    - "README.md contains no <details> or <summary> tags anywhere"
    - "README.md no longer contains a Quick Start section (replaced per D-04)"
    - "README.md's Table of Contents links to every top-level section and to every #### template entry"
    - "The four Wave 1 fragments are spliced verbatim into README.md (no semantic edits)"
    - "Pre-existing well-targeted sections (Why this exists, Features at a glance, How it works, Repository Layout, Extending the system, Conventions, Troubleshooting, FAQ, License) are preserved"
    - "FAQ contains one new Q&A about the 'raw' status / Update Note Status known issue"
  artifacts:
    - path: "README.md"
      provides: "Complete rewritten student-facing README"
      contains: "## Setup Guide"
  key_links:
    - from: "README Table of Contents"
      to: "every section heading via anchor"
      via: "Markdown anchor links"
      pattern: "\\[.*\\]\\(#"
---

## Phase Goal

**As a** fellow student picking up this vault for the first time, **I want to** read a single coherent README, **so that** I can install, configure, learn the workflow, and look up any template without ever touching source code.

<objective>
Integrate the four Wave 1 section fragments into `README.md` in the D-03 section order, replacing the obsolete Quick Start and Configure & Customize sections, preserving the well-targeted existing sections, removing both `<details>` blocks, and refreshing the Table of Contents. After this plan, README.md is the final deliverable for Phase 2.

Purpose: DOCS-05 — assemble all documentation improvements into a single coherent README.
Output: Rewritten `README.md` ready for commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-documentation-overhaul/02-CONTEXT.md
@.planning/phases/02-documentation-overhaul/02-RESEARCH.md
@.planning/phases/02-documentation-overhaul/fragments/setup-guide.md
@.planning/phases/02-documentation-overhaul/fragments/config-guide.md
@.planning/phases/02-documentation-overhaul/fragments/walkthrough.md
@.planning/phases/02-documentation-overhaul/fragments/template-reference.md
@CLAUDE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble README.md from fragments in D-03 section order</name>
  <files>README.md</files>
  <read_first>
    - README.md (FULL — current; need to identify which existing sections survive and where the legacy Quick Start / Configure &amp; Customize / Usage / `<details>` blocks live)
    - NOTE: CLAUDE.md lists README under "Do not change" as a general guard against casual edits to project architecture. This phase is the explicitly commissioned documentation overhaul (Phase 2 goal) and the phase mandate takes precedence over that general-purpose guard. All other "Do not change" items (architecture, helper pattern, config-driven design, Dataview queries, resolveSubjectParcialTema, ensureFolderPath, ensureUniqueFileName) remain protected and are not touched by this plan.
    - .planning/phases/02-documentation-overhaul/fragments/setup-guide.md (FULL — splice verbatim)
    - .planning/phases/02-documentation-overhaul/fragments/config-guide.md (FULL — splice verbatim)
    - .planning/phases/02-documentation-overhaul/fragments/walkthrough.md (FULL — splice verbatim)
    - .planning/phases/02-documentation-overhaul/fragments/template-reference.md (FULL — splice verbatim)
    - .planning/phases/02-documentation-overhaul/02-CONTEXT.md (decisions D-01 through D-08)
    - .planning/phases/02-documentation-overhaul/02-RESEARCH.md (section "Recommended Section Structure for New README" gives the final outline)
  </read_first>
  <action>
    Rewrite `README.md` so its top-to-bottom structure is exactly:

    1. `# Obsidian University Workflow` title + the existing tagline blockquote (preserve verbatim from current README lines 1-3)
    2. Updated Table of Contents — a flat bullet list of anchor links. MUST list (in this order): TL;DR, Why this exists, Features at a glance, Setup Guide, Configuration Guide, Workflow Walkthrough, Template Reference (with nested anchor sub-bullets for the 11 template names exactly as they appear in the Template Reference fragment headings), How it works, Repository Layout, Extending the system, Conventions, Troubleshooting, FAQ, License. Use standard Markdown auto-anchors (lowercase, spaces -> hyphens, strip punctuation).
    3. The three existing license/plugin badge shields line (preserve verbatim from current README line 28).
    4. `## TL;DR` section — preserve verbatim from current README.
    5. `## Why this exists (problem -> solution in plain language)` section — preserve verbatim.
    6. `## Features at a glance` section — preserve verbatim.
    7. **Splice in `fragments/setup-guide.md` verbatim** — this REPLACES the old `## Quick Start` section. Delete the old Quick Start entirely.
    8. **Splice in `fragments/config-guide.md` verbatim** — this REPLACES the old `## Configure & Customize` section (including the `<details>` "Common tweaks" block) and the trailing paragraph "Because templates read the config at runtime...". Delete all of that.
    9. **Splice in `fragments/walkthrough.md` verbatim** — new section, no replacement; positioned between Config Guide and Template Reference per D-03.
    10. **Splice in `fragments/template-reference.md` verbatim** — this REPLACES the old `## Usage` section (lines 130-291 in the current README, including the `<details>` "Template prompts quick reference" block). Delete all of that.
    11. `## How it works` section — preserve verbatim (per the RESEARCH.md recommendation, place it after Template Reference; OPEN QUESTION 2 in research suggested after Setup, but D-03 fixed the order ending at Troubleshooting/FAQ — place it here as a deeper-internals section after Template Reference and before Repository Layout).
    12. `## Repository Layout` section — preserve verbatim BUT ensure the `_templates/` listing includes all 11 templates. Add the two missing rows: `Mark Reviewed.md` and `University Dashboard.md` (with one-line descriptions matching the Template Reference entries).
    13. `## Extending the system` section — preserve verbatim.
    14. `## Conventions` section — preserve verbatim. Verify the `type` key row mentions all six type values including `university-dashboard` (current README only lists five — add `university-dashboard` to the parenthetical list to match `schema.types` in universityConfig.js).
    15. `## Troubleshooting` section — preserve verbatim.
    16. `## FAQ` section — preserve verbatim BUT append one new Q&A at the end of the FAQ list, before the License section:
        `**Why don't my notes with status: raw show up in Update Note Status?** This is a known issue — the picker's fallback status list omits "raw". Update the affected notes' frontmatter manually until a fix is released. Tracked in AUDIT.md.`
    17. `## License` section — preserve verbatim.

    Mandatory final-state checks (the executor MUST verify before considering the task done):
    - No `<details>` tags anywhere in README.md.
    - No `<summary>` tags anywhere in README.md.
    - No `## Quick Start` section.
    - No `## Configure & Customize` section.
    - No `## Usage` section.
    - The string `## Setup Guide` appears exactly once.
    - The string `## Configuration Guide` appears exactly once.
    - The string `## Workflow Walkthrough` appears exactly once.
    - The string `## Template Reference` appears exactly once.
    - All 11 template names (`Lecture Note`, `Concept Note Template`, `General Note`, `Subject Hub`, `Parcial Prep Note`, `Assign Tema to Current Note`, `Link Concepts to Note`, `Quick Create Concept`, `Update Note Status`, `Mark Reviewed`, `University Dashboard`) appear in README.md.

    Splicing rule: when inserting a fragment, copy its content verbatim from the fragment file. Do not edit fragment prose during assembly. If you find a real defect in a fragment (e.g., a typo or a missing required string), fix it in the fragment file first, then splice.
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; ! grep -q "&lt;details&gt;" README.md &amp;&amp; ! grep -q "&lt;summary&gt;" README.md &amp;&amp; ! grep -q "^## Quick Start" README.md &amp;&amp; ! grep -q "^## Configure &amp; Customize" README.md &amp;&amp; ! grep -q "^## Usage$" README.md &amp;&amp; [ "$(grep -c '^## Setup Guide$' README.md)" -eq 1 ] &amp;&amp; [ "$(grep -c '^## Configuration Guide$' README.md)" -eq 1 ] &amp;&amp; [ "$(grep -c '^## Workflow Walkthrough$' README.md)" -eq 1 ] &amp;&amp; [ "$(grep -c '^## Template Reference$' README.md)" -eq 1 ] &amp;&amp; grep -q "Lecture Note" README.md &amp;&amp; grep -q "Concept Note Template" README.md &amp;&amp; grep -q "General Note" README.md &amp;&amp; grep -q "Subject Hub" README.md &amp;&amp; grep -q "Parcial Prep Note" README.md &amp;&amp; grep -q "Assign Tema to Current Note" README.md &amp;&amp; grep -q "Link Concepts to Note" README.md &amp;&amp; grep -q "Quick Create Concept" README.md &amp;&amp; grep -q "Update Note Status" README.md &amp;&amp; grep -q "Mark Reviewed" README.md &amp;&amp; grep -q "University Dashboard" README.md &amp;&amp; grep -q "status: raw" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists and contains a Table of Contents with anchor links to all top-level sections in D-03 order
    - README.md does NOT contain any `<details>` or `<summary>` tags
    - README.md does NOT contain `## Quick Start`, `## Configure & Customize`, or `## Usage` (with exact `## ` prefix)
    - README.md contains exactly one occurrence of each of these section headings (line-anchored): `## Setup Guide`, `## Configuration Guide`, `## Workflow Walkthrough`, `## Template Reference`
    - All 11 template names appear in README.md (substring match): `Lecture Note`, `Concept Note Template`, `General Note`, `Subject Hub`, `Parcial Prep Note`, `Assign Tema to Current Note`, `Link Concepts to Note`, `Quick Create Concept`, `Update Note Status`, `Mark Reviewed`, `University Dashboard`
    - Section order verified manually: TL;DR -> Why this exists -> Features at a glance -> Setup Guide -> Configuration Guide -> Workflow Walkthrough -> Template Reference -> How it works -> Repository Layout -> Extending the system -> Conventions -> Troubleshooting -> FAQ -> License
    - Repository Layout `_templates/` listing includes lines for `Mark Reviewed.md` and `University Dashboard.md`
    - Conventions `type` row mentions `university-dashboard` value
    - FAQ contains the new Q&A about `status: raw` and Update Note Status
    - Pre-existing preserved sections (TL;DR, Why this exists, Features at a glance, How it works, Extending the system, Conventions, Troubleshooting, License) retain their content from the prior README
  </acceptance_criteria>
  <done>README.md is a single coherent guide structured for a fellow student — install, configure, see-it-in-action, look-up — with no collapsibles, no orphaned legacy sections, and all 11 templates documented.</done>
</task>

</tasks>

<verification>
- All five Phase 2 success criteria from ROADMAP.md verifiable from README.md:
  1. Plugin setup section with Templater + Dataview steps — Setup Guide section
  2. Config guide with worked example — Configuration Guide section
  3. Workflow walkthrough end-to-end — Workflow Walkthrough section
  4. Template reference covers all 11 templates — Template Reference section
  5. Coherent guide for a new student — overall README flow in D-03 order
  6. Audit findings referenced where notable — two callouts in Template Reference + one FAQ entry
- All acceptance criteria above pass
- The automated grep checks verify the structural invariants
</verification>

<success_criteria>
DOCS-05 satisfied. All five Phase 2 requirements (DOCS-01 through DOCS-05) are now reflected in the live `README.md`. Phase 2 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-overhaul/02-05-SUMMARY.md` documenting: README rewritten, line count delta, sections preserved vs replaced, any fragment fixes applied during splice.
</output>
