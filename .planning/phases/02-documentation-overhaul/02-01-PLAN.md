---
phase: 02-documentation-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-documentation-overhaul/fragments/setup-guide.md
autonomous: true
requirements:
  - DOCS-01
tags:
  - documentation
  - obsidian
  - templater
  - setup

must_haves:
  truths:
    - "A new student can install Templater following the doc without reading source code"
    - "A new student can install Dataview following the doc without reading source code"
    - "The doc states the exact Templater settings to set Template folder to _templates and Script folder to _templater_scripts"
    - "The doc explains how to reload Templater user scripts so universityConfig, getUniversityContext, universityNoteUtils, scriptLoader, templateBootstrap register"
    - "The doc states the Templater minimum version (2.16) and why"
    - "The doc states the desktop-only constraint"
  artifacts:
    - path: ".planning/phases/02-documentation-overhaul/fragments/setup-guide.md"
      provides: "Self-contained Setup Guide section ready to splice into README.md"
      contains: "## Setup Guide"
  key_links:
    - from: "Setup Guide steps"
      to: "Templater plugin settings (Template folder location, Script files folder location)"
      via: "exact menu path text"
      pattern: "Template folder location.*_templates"
---

## Phase Goal

**As a** fellow student new to this vault, **I want to** install and configure Templater + Dataview so the templates work end-to-end, **so that** I can run my first Lecture Note without reading the source code.

<objective>
Write the Setup Guide section (DOCS-01) as a self-contained Markdown fragment. This fragment is the **first vertical slice** of the new README — when complete, a student can use it standalone (paste into a new Obsidian note) to bring the workflow online before any other doc section is ready.

Purpose: DOCS-01 — replace the terse 5-line Quick Start with a named-step setup sequence covering install -> folder config -> reload -> Dataview enable, exactly as required by D-04.
Output: `.planning/phases/02-documentation-overhaul/fragments/setup-guide.md` — a complete `## Setup Guide` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-documentation-overhaul/02-CONTEXT.md
@.planning/phases/02-documentation-overhaul/02-RESEARCH.md
@CLAUDE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Setup Guide fragment</name>
  <files>.planning/phases/02-documentation-overhaul/fragments/setup-guide.md</files>
  <read_first>
    - README.md (current — read Quick Start section lines 58-70 to understand what is being replaced; read TL;DR / Features at a glance for tone alignment)
    - .planning/phases/02-documentation-overhaul/02-CONTEXT.md (decisions D-01, D-02, D-03, D-04)
    - .planning/phases/02-documentation-overhaul/02-RESEARCH.md (section "Setup Guide Content (DOCS-01)" gives the exact 6-step sequence)
    - CLAUDE.md (project — reader persona and jargon rules)
  </read_first>
  <action>
    Create the file `.planning/phases/02-documentation-overhaul/fragments/setup-guide.md` containing the **complete** `## Setup Guide` section.

    The fragment MUST contain, in this order:

    1. Top-level heading: `## Setup Guide`
    2. A one-paragraph intro stating the goal: "By the end of this section, you will have installed Templater and Dataview, told Templater where to find this vault's templates and scripts, and verified the setup by creating a test Lecture Note."
    3. A `### Prerequisites` subsection listing: (a) Obsidian desktop (state explicitly that mobile is unsupported because `tp.user.*` scripts cannot run on Obsidian mobile); (b) Community plugins enabled in Obsidian settings.
    4. Six named-step subsections — each `### Step N: ...`:
       - `### Step 1: Install Templater` — exact UI path "Settings -> Community Plugins -> Browse -> search Templater -> Install -> Enable".
       - `### Step 2: Install Dataview` — same pattern, with Dataview.
       - `### Step 3: Copy the workflow files into your vault` — copy `_templates/` and `_templater_scripts/` folders into the vault root (alongside `.obsidian/`).
       - `### Step 4: Configure Templater` — exact settings: "Settings -> Templater -> Template folder location: `_templates`" and "Settings -> Templater -> Script files folder location: `_templater_scripts`". Mention the optional "Trigger Templater on new file creation" toggle.
       - `### Step 5: Reload Templater user scripts` — command palette "Templater: Replace templates in the active file" OR restart Obsidian. State that this registers the five user scripts by name: `universityConfig`, `getUniversityContext`, `universityNoteUtils`, `scriptLoader`, `templateBootstrap`.
       - `### Step 6: Verify the setup` — create a new untitled note, command palette "Templater: Create new note from template", select "Lecture Note", confirm year/subject/tema prompts appear.
    5. A `### Version requirement` subsection stating: Templater >= 2.16 required for `tp.system.multi_suggester` (used by Lecture Note concept tagging, Link Concepts to Note, Update Note Status). Older versions still work but multi-select features fall back silently.

    Tone & content rules (mandatory):
    - No `<details>` or `<summary>` tags anywhere (per D-02).
    - No developer jargon. Forbidden tokens in the fragment text: `CommonJS`, `module.exports`, `factory function`, `EROFS`, `async/await`, `require()`. Obsidian UI terms (command palette, frontmatter, community plugins, settings) are fine.
    - Wrap exact UI text and folder paths in backticks (e.g., `Settings -> Community Plugins`, `_templates/`).
    - Do not invent steps that are not in the RESEARCH.md "Setup Guide Content (DOCS-01)" section. Do not omit any of the 6 steps.
    - Do not include any other section (no Config Guide, no Walkthrough). This fragment is ONLY the Setup Guide.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-documentation-overhaul/fragments/setup-guide.md &amp;&amp; grep -q "^## Setup Guide$" .planning/phases/02-documentation-overhaul/fragments/setup-guide.md &amp;&amp; grep -q "### Step 1: Install Templater" .planning/phases/02-documentation-overhaul/fragments/setup-guide.md &amp;&amp; grep -q "### Step 6: Verify" .planning/phases/02-documentation-overhaul/fragments/setup-guide.md &amp;&amp; grep -q "_templates" .planning/phases/02-documentation-overhaul/fragments/setup-guide.md &amp;&amp; grep -q "_templater_scripts" .planning/phases/02-documentation-overhaul/fragments/setup-guide.md &amp;&amp; grep -q "2.16" .planning/phases/02-documentation-overhaul/fragments/setup-guide.md &amp;&amp; ! grep -q "&lt;details&gt;" .planning/phases/02-documentation-overhaul/fragments/setup-guide.md &amp;&amp; ! grep -qE "CommonJS|module\.exports|EROFS" .planning/phases/02-documentation-overhaul/fragments/setup-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-documentation-overhaul/fragments/setup-guide.md` exists
    - File contains the heading line `## Setup Guide` (exact match)
    - File contains a `### Prerequisites` subsection
    - File contains all six step headings: `### Step 1: Install Templater`, `### Step 2: Install Dataview`, `### Step 3: Copy the workflow files into your vault`, `### Step 4: Configure Templater`, `### Step 5: Reload Templater user scripts`, `### Step 6: Verify the setup`
    - Step 4 contains the literal strings `_templates` and `_templater_scripts` and `Template folder location` and `Script files folder location`
    - Step 5 names all five scripts: `universityConfig`, `getUniversityContext`, `universityNoteUtils`, `scriptLoader`, `templateBootstrap`
    - File contains a Version requirement note mentioning `2.16` and `tp.system.multi_suggester`
    - File does NOT contain `<details>` or `<summary>` tags
    - File does NOT contain the tokens `CommonJS`, `module.exports`, or `EROFS`
    - File contains no section headings outside the Setup Guide scope (no `## Config Guide`, no `## Walkthrough`, no `## Template Reference`)
  </acceptance_criteria>
  <done>The Setup Guide fragment is a standalone Markdown section that a student could paste into any document and follow end-to-end to bring the workflow online.</done>
</task>

</tasks>

<verification>
- Fragment file exists at the expected path
- All acceptance criteria above pass
- Manual scan: a student reading only this fragment can install both plugins, point Templater at the right folders, reload scripts, and verify with a test note
</verification>

<success_criteria>
DOCS-01 satisfied as a standalone section. Phase 2 success criterion #1 ("Plugin setup section exists with exact steps for Templater folder config and Dataview enable") is achievable from this fragment.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-overhaul/02-01-SUMMARY.md` documenting: file written, sections included, any deviations from RESEARCH.md, any open follow-ups for the assembly plan.
</output>
