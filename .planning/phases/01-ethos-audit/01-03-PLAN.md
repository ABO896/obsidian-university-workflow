---
phase: 01-ethos-audit
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - AUDIT.md
autonomous: true
gap_closure: true
requirements:
  - AUDIT-01
  - AUDIT-03

must_haves:
  truths:
    - "The Section 1 Critical fix instruction for Update Note Status.md uses `schema?.statuses` (not `config?.schema?.statuses`)"
    - "The Section 4 item 2 fix instruction for Update Note Status.md uses `schema?.statuses` (not `config?.schema?.statuses`)"
    - "Section 3 Minor contains an entry for `_templates/General Note.md` line 49 with `promptYearWhen: \"always\"` and fix to change it to `\"missing\"`"
    - "The footer minor violation count reads 22 (not 19)"
  artifacts:
    - path: "AUDIT.md"
      provides: "Corrected audit report with accurate fix instructions and complete Section 3"
      contains: "schema?.statuses"
  key_links:
    - from: "AUDIT.md Section 1 Critical"
      to: "_templates/Update Note Status.md"
      via: "fix instruction variable reference"
      pattern: "schema\\?\\.statuses"
    - from: "AUDIT.md Section 3 Minor"
      to: "_templates/General Note.md"
      via: "promptYearWhen violation entry"
      pattern: "General Note\\.md.*line 49"
---

<objective>
Close two accuracy defects found in AUDIT.md during Phase 01 verification:

1. CR-01 (BLOCKER): The fix instruction for `Update Note Status.md` in Section 1 Critical and Section 4 item 2 references `config?.schema?.statuses` — but `config` is not in scope in that template. The template destructures `const { noteUtils, schema } = ctx`, so `schema` is the correct variable. Following the fix as written would cause a ReferenceError.

2. CR-02 (WARNING): Section 3 (Zero Friction) omits `General Note.md` line 49 (`promptYearWhen: "always"`), which is the same violation documented three times in the same section. The footer minor count (19) is also wrong — the body contains 21 (S1: 10, S2: 6, S3: 5); after adding the missing entry it becomes 22.

Purpose: Ensure every fix instruction in AUDIT.md is accurate and actionable, and that Section 3 is complete as a fix backlog.
Output: AUDIT.md with two targeted edits — no structural changes to any other section.
</objective>

<execution_context>
@/Users/alvaro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alvaro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ethos-audit/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-01 — Correct `config` → `schema` in both fix instructions</name>
  <files>AUDIT.md</files>
  <read_first>
    Read AUDIT.md in full before making any edits. Confirm the exact text at line 13 (Section 1 Critical fix) and line 81 (Section 4 item 2 fix). The lines to change contain the string `config?.schema?.statuses` — locate both occurrences before touching the file.
  </read_first>
  <action>
    Make exactly two string replacements in AUDIT.md. Do NOT alter any surrounding text, formatting, or whitespace.

    Replacement 1 — Section 1 Critical (line 13):
    Find: `config?.schema?.statuses ?? ["raw", "draft", "reviewed", "complete"]`
    Replace with: `schema?.statuses ?? ["raw", "draft", "reviewed", "complete"]`

    Replacement 2 — Section 4 item 2 (line 81):
    Find: `config?.schema?.statuses ?? ["raw", "draft", "reviewed", "complete"]`
    Replace with: `schema?.statuses ?? ["raw", "draft", "reviewed", "complete"]`

    Both occurrences use identical text, so verify BOTH are corrected — not just the first match. The string `config?.schema?.statuses` must not appear anywhere in AUDIT.md after this edit.
  </action>
  <verify>
    <automated>grep -c "config?.schema?.statuses" /Users/alvaro/alvaroVault/.claude/worktrees/happy-wu-52173e/AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "config?.schema?.statuses" AUDIT.md` returns `0`
    - `grep -c "schema?.statuses" AUDIT.md` returns `2` (one in Section 1, one in Section 4)
    - The Section 1 Critical bullet for `Update Note Status.md` still references line 26 and still describes the "raw" omission — only the fix expression changed
    - The Section 4 item 2 bullet still reads "Update Note Status missing 'raw' in fallback statuses" — only the fix expression changed
  </acceptance_criteria>
  <done>Neither occurrence of `config?.schema?.statuses` remains in AUDIT.md. Both are replaced with `schema?.statuses`. No other content is altered.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-02 — Add General Note.md entry to Section 3 and correct footer count</name>
  <files>AUDIT.md</files>
  <read_first>
    Read AUDIT.md again after Task 1 completes. Locate:
    (a) The end of Section 3 Minor — find the last bullet in Section 3 (the `Quick Create Concept.md` entry for `promptYearWhen: "missing"`) and the blank line before `## 4.`.
    (b) The footer line (last line of file) that currently reads `19 minor violations`.
    Confirm the exact surrounding text before inserting or replacing.
  </read_first>
  <action>
    Make exactly two edits to AUDIT.md.

    Edit 1 — Insert a new Minor bullet at the END of Section 3, immediately before the blank line that precedes `## 4. Expansion Opportunities`.

    The new bullet to insert (verbatim):

    - **`_templates/General Note.md` line 49:** `promptYearWhen: "always"` — year is prompted even when the current note already has a year in its frontmatter. This is the same unnecessary confirmation step documented for Assign Tema, Concept Note Template, and Lecture Note. → **Fix:** Change to `promptYearWhen: "missing"` so the prompt is skipped when year context is already available from the current note.

    Insert this as a new line (with a blank line between it and the `## 4.` heading). Do NOT modify any existing Section 3 bullets.

    Edit 2 — Footer line update (last line of file):
    Find: `19 minor violations`
    Replace with: `22 minor violations`

    The footer currently reads:
    `_Audit generated: 2026-05-15 | 16 files reviewed | 2 critical violations | 19 minor violations | 9 expansion opportunities_`

    After edit it must read:
    `_Audit generated: 2026-05-15 | 16 files reviewed | 2 critical violations | 22 minor violations | 9 expansion opportunities_`
  </action>
  <verify>
    <automated>grep -n "General Note.md.*line 49" /Users/alvaro/alvaroVault/.claude/worktrees/happy-wu-52173e/AUDIT.md && grep "22 minor violations" /Users/alvaro/alvaroVault/.claude/worktrees/happy-wu-52173e/AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "General Note.md.*line 49" AUDIT.md` returns `1`
    - The new bullet appears inside Section 3 (after the `Lecture Note.md` entry and before `## 4.`)
    - The new bullet contains the text `promptYearWhen: "always"` and the fix `promptYearWhen: "missing"`
    - `grep "minor violations" AUDIT.md` returns a line containing `22 minor violations`
    - The string `19 minor violations` does not appear in AUDIT.md
    - Section 3 now contains 6 Minor bullets (Assign Tema, Concept Note Template, General Note lines 22-38, Lecture Note, Quick Create Concept, General Note line 49)
    - No other section is modified
  </acceptance_criteria>
  <done>Section 3 Minor has 6 entries including `General Note.md` line 49. Footer reports 22 minor violations. The string `19 minor violations` is absent from AUDIT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → executor | Executor receives exact string replacements; no ambiguity in target text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | AUDIT.md edits | accept | Targeted string replacements with grep-verifiable acceptance criteria; low risk for a markdown doc edit |
</threat_model>

<verification>
After both tasks complete, run:

```bash
# CR-01 closed: no bad variable remains
grep -c "config?.schema?.statuses" AUDIT.md   # must return 0

# CR-01 fix present in both locations
grep -c "schema?.statuses" AUDIT.md           # must return 2

# CR-02 closed: General Note entry present in Section 3
grep -n "General Note.md.*line 49" AUDIT.md   # must return 1 match inside Section 3

# Footer correct
grep "minor violations" AUDIT.md              # must contain "22 minor violations"
```
</verification>

<success_criteria>
- AUDIT.md contains zero occurrences of `config?.schema?.statuses`
- AUDIT.md contains exactly two occurrences of `schema?.statuses` (Section 1 Critical and Section 4 item 2)
- Section 3 Minor contains an entry for `_templates/General Note.md` line 49 with `promptYearWhen: "always"` and fix to `"missing"`
- Footer minor violation count is 22
- No other content in AUDIT.md is altered
- AUDIT.md is committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/01-ethos-audit/01-03-SUMMARY.md` using the summary template.
</output>
